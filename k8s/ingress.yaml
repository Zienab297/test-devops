apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: devops-test-main
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: devops-test-main
                port:
                  number: 8000