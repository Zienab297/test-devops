version: "3.9"
services:
  devops-test-main:
    build: .
    container_name: devops-test-main
    ports:
      - "8000/8000"
    restart: unless-stopped
    env_file:
      - .env

  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: devops-test-main
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data: